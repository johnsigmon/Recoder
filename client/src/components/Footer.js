import React, { Component } from 'react';
import { Row } from 'react-bootstrap';
export default class Footer extends Component {
  render() {
    return (
      <Row className="display" style={{backgroundColor: 'lightblue', padding: '0', margin: '0'}}>
        Footer
      </Row>
    );
  }
}
